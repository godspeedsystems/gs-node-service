type: api
schema:
base_url: <% config.growthsource.base_url %>
security:
  - ApiKey: plpinelabs
  - ApiToken: <% config.growthsource.api_token %>

securitySchemes:
  ApiKey:
    type: apiKey
    in: header
    name: x-api-key

  ApiToken:
    type: apiKey
    in: header
    name: Authorization